apiVersion: v1
kind: Service
metadata:
  name: "service-2048"
  namespace: "2048-game"
spec:
  ports:
    - port: 8383
      targetPort: 80
      protocol: TCP
  type: LoadBalancer
  selector:
    app: "2048"

